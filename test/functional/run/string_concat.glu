//
// RUN: gluc --sanitize-address %s -o %t && %t | FileCheck -v %s
//

func main() -> Int {
    var asd: String = "hello";
    // CHECK: hello
    std::print(asd);
    // CHECK-NEXT: hello world
    std::print(asd + " world");
    return 0;
}
