//
// RUN: gluc %s -o %t && %t | FileCheck -v %s
//

import string;

func main() -> Int {
    var asd: String = "hello";
    // CHECK: hello
    std::print(asd);
    // CHECK-NEXT: hello world
    std::print(asd + " world");
    return 0;
}
