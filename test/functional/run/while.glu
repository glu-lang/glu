//
// RUN: gluc -c %s -o %t.o && clang -o %t %t.o && %t | FileCheck -v %s
//

func puts(s: *Char);

func testWhile() -> Void {
    var flag: Bool = true;

    while flag {
        puts("yes!");
        flag = false;
    }
}

// CHECK: yes!
func main() -> Int {
    testWhile();
    return 0;
}
