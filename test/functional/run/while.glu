//
// RUN: gluc %s -o %t && %t | FileCheck -v %s
//

@no_mangling func puts(s: *Char);

func testWhile() -> Void {
    var flag: Bool = true;

    while flag {
        puts("yes!");
        flag = false;
    }
}

// CHECK: yes!
func main() -> Int {
    testWhile();
    return 0;
}
