//
// RUN: gluc %s -o %t && %t | FileCheck -v %s
//

@no_mangling func puts(s: *Char);

func testBreak() -> Void {
    var flag: Bool = true;

    while flag {
        puts("yes!");
        break;
    }
}

// CHECK: yes!
func main() -> Int {
    testBreak();
    return 0;
}
