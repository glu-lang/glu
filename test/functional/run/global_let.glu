//
// RUN: gluc %s -o %t && %t | FileCheck -v %s
//

let globalFlag: Bool = true;
let globalName: String = "global";

// CHECK: global
func main() -> Int {
    if globalFlag {
        std::print(globalName);
    } else {
        std::print("no!");
    }
    return 0;
}
