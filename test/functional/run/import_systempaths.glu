//
// RUN: gluc %s -o %t && %t | FileCheck -v %s
// REQUIRES: gluc_linking
//

import glucinfo;

func puts(s: *Char);

// CHECK: alpha
func main() -> Int {
    puts(glucinfo::getVersion());
    return 0;
}
