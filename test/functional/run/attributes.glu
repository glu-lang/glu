//
// RUN: gluc %s -o %t && %t | FileCheck -v %s
//

@no_mangling @c_variadic func printf(s: *Char) -> Int;

@packed @alignment(16) struct MyStruct {
    mySIMDDoubles: UInt8,
    enabled: Bool,
    misalignedInt: Int
}

func main() {
    let s: MyStruct = { 16, true, 8 };

    printf("misaligned int = %d\n", s.misalignedInt);
}
