# Local configuration for Odin IRDec tests
# Allow .odin files

import lit.util

config.suffixes.add('.odin')

# Mark unsupported if Odin is not available
if not lit.util.which("odin", config.environment["PATH"]):
  config.unsupported = True
