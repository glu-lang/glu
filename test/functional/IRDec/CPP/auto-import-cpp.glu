//
// RUN: gluc %s --linker clang++ -o %t && %t | FileCheck -v %s
//

import simple;

func main() {
    // CHECK: Pizza count is 42, compiler is Clang++
    let manager: simple::PizzaManager = {};
    std::printf(
        "Pizza count is %d, compiler is %s\n",
        simple::PizzaManager::getPizzaCount(&manager), simple::getCC()
    );
}
