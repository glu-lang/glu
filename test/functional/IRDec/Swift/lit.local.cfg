# Local configuration for Swift IRDec tests
# Allow .swift files

import lit.util

config.suffixes.add('.swift')

# Mark unsupported if Swift is not available
if not lit.util.which("swiftc", config.environment["PATH"]):
  config.unsupported = True
