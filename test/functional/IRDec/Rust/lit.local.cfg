# Local configuration for Rust IRDec tests
# Allow .rs files

import lit.util

config.suffixes.add('.rs')

# Mark unsupported if Rustc is not available
if not lit.util.which("rustc", config.environment["PATH"]):
  config.unsupported = True
