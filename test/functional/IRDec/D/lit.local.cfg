# Local configuration for D IRDec tests
# Allow .d files

import lit.util

config.suffixes.add('.d')

# Mark unsupported if LDC is not available
if not lit.util.which("ldc2", config.environment["PATH"]):
  config.unsupported = True
