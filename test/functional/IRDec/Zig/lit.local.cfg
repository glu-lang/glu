# Local configuration for Zig IRDec tests
# Allow .zig files

import lit.util

config.suffixes.add('.zig')

# Mark unsupported if Zig is not available
if not lit.util.which("zig", config.environment["PATH"]):
  config.unsupported = True
