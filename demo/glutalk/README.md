# GluTalk Demo

This demo stitches Glu, C++, and Rust into a single executable.

## Usage
From the repository root:

```sh
# Make sure LLVM20 is at the front of your PATH
cd demo/glutalk
make
make run
```

Use `make clean` to remove build artifacts.
